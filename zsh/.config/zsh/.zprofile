################################################################################
# => Start menu on tty1
################################################################################

if [[ "$(tty)" = "/dev/tty1" ]]; then
	source $HOME/.config/zsh/path.zsh
	$HOME/.scripts/start-menu
fi
